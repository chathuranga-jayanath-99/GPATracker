<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gpa_tracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPATracker">
        <activity
            android:name=".ui.MarkResultActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ModulesAdapterViewLayout"
            android:exported="false" />
        <activity
            android:name=".ui.SemesterSubjectActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AccountSemesterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>